<%= form_for(@set_list) do |f| %>
  <% if @set_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@set_list.errors.count, "error") %> prohibited this set_list from being saved:</h2>

      <ul>
      <% @set_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :target_length_in_seconds %><br>
    <%= f.number_field :target_length_in_seconds %><br>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
